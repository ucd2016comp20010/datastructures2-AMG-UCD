Q5 
    Call a helper function
    Check recursively though all heights, in the left and right node
    Compare current largest found diameter with the newest createNode
    if new is larger, it becomes the largest found
    return the largest of the left and right height